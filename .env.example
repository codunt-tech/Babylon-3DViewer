# Base URL for all 3D model GLB assets.
# Leave empty (or unset) to serve from the local public/ folder.
# Set to a CDN or remote host if models are hosted externally.
# Example: https://cdn.example.com/fpso-models
VITE_MODELS_BASE_URL=
